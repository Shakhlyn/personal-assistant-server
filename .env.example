# Secrets — generate with: openssl rand -hex 32
SECRET_KEY=replace-me

# Database
DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/book_mgt

# Local development toggles
DEBUG=true
DB_ECHO=false

# Connection pool (per worker process)
DB_POOL_SIZE=5
DB_MAX_OVERFLOW=10
